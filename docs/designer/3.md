# 第 3 轮迭代技术方案 (N=3)

**设计方案**: Designer Agent  
**迭代版本**: N=3  
**日期**: 2026-04-09  
**状态**: 设计阶段

---

## 1. 本轮迭代目标

### 1.1 修复问题清单

| ID | 问题 | 来源 | 优先级 |
|----|------|------|--------|
| T1 | 15 个测试因缺少 @pytest.mark.asyncio 失败 | tester/2.md | 高 |
| T2 | AgentEvent 允许额外字段（期望禁止）| tester/2.md | 低 |
| T3 | ChatResult 不是 frozen（期望不可变）| tester/2.md | 低 |
| T4 | TokenStats.elapsed 计算逻辑错误 | tester/2.md | 低 |
| F1 | temperature/max_tokens 未传递给 Provider | approval/2.md | 高 |

### 1.2 新增功能清单

| ID | 功能 | 说明 | 优先级 |
|----|------|------|--------|
| P1 | OllamaProvider 实现 | 本地模型支持 | 中 |
| P2 | 内置工具扩展 | Calculator + File 工具 | 中 |

---

## 2. 详细设计规范

### 2.1 pytest-asyncio 配置修复

**问题分析**:
- `test_improvements.py` 和 `test_integration.py` 中的 async 测试函数缺少 `@pytest.mark.asyncio` 装饰器
- 共 15 个测试失败 (11 + 6)

**解决方案** (推荐方案 A - 全局配置):

```toml
# pyproject.toml 添加
[tool.pytest.ini_options]
asyncio_mode = "auto"
```

此配置会自动将所有 async 测试函数视为 pytest-asyncio 测试，无需逐个添加装饰器。

**备选方案** (方案 B - 装饰器):
如果全局配置有副作用，可以为每个 async 测试函数添加装饰器:

```python
import pytest

@pytest.mark.asyncio
async def test_event_stream_basic():
    ...
```

**实现要求**:
1. 修改 `pyproject.toml` 添加 asyncio_mode 配置
2. 运行测试验证 15 个失败的测试是否通过
3. 确保其他测试不受影响

---

### 2.2 temperature/max_tokens 参数传递

**问题分析**:
- `AgentLoopConfig` 已定义 `temperature` 和 `max_tokens` 字段
- 但 `_stream_assistant_response` 函数调用 `provider.stream_chat()` 时未传递这些参数
- Provider 的 `stream_chat` 方法签名目前不接受这些参数

**解决方案**:

**步骤 1**: 修改 Provider 基类 `src/tui_chatbot/provider/base.py`

在 `stream_chat` 方法签名中添加 `temperature` 和 `max_tokens` 参数:

```python
@abstractmethod
async def stream_chat(
    self,
    model: str,
    messages: List[dict],
    tools: Optional[List[dict]] = None,
    signal: Optional["AbortSignal"] = None,
    temperature: Optional[float] = None,  # 新增
    max_tokens: Optional[int] = None,         # 新增
) -> "EventStream[AgentEvent, ChatResult]":
    pass
```

**步骤 2**: 修改 OpenAIProvider `src/tui_chatbot/provider/openai_provider.py`

更新 `stream_chat` 方法实现，使用传入的参数覆盖配置中的参数:

```python
async def stream_chat(
    self,
    model: str,
    messages: List[dict],
    tools: Optional[List[dict]] = None,
    signal: Optional["AbortSignal"] = None,
    temperature: Optional[float] = None,
    max_tokens: Optional[int] = None,
) -> EventStream[AgentEvent, ChatResult]:
    # 使用传入的参数，如果为 None 则使用配置中的值
    effective_temperature = temperature if temperature is not None else self._config.temperature
    effective_max_tokens = max_tokens if max_tokens is not None else self._config.max_tokens
    
    # 在构建 create_params 时使用 effective_* 变量
```

**步骤 3**: 修改 AnthropicProvider `src/tui_chatbot/provider/anthropic_provider.py`

同样方式更新参数处理逻辑。

**步骤 4**: 修改 Agent Loop `src/tui_chatbot/agent/loop.py`

在 `_stream_assistant_response` 函数中传递参数:

```python
stream = await provider.stream_chat(
    model=config.model,
    messages=provider_messages,
    tools=tools_schema,
    signal=signal,
    temperature=config.temperature,  # 新增
    max_tokens=config.max_tokens,    # 新增
)
```

---

### 2.3 OllamaProvider 设计

**参考实现**: `src/tui_chatbot/provider/openai_provider.py`

**设计要点**:
- Ollama API 兼容 OpenAI API 格式
- 使用 `httpx` 或 `aiohttp` 进行 HTTP 请求（无需额外依赖）
- 支持本地模型部署

**文件创建**: `src/tui_chatbot/provider/ollama_provider.py`

```python
"""Ollama Provider 实现 - 本地模型支持."""

from __future__ import annotations

import asyncio
import json
import os
from typing import TYPE_CHECKING, Any, Dict, List, Optional

from pydantic import ConfigDict, Field

from ..agent.types import (
    AgentEvent,
    AgentEventType,
    AssistantMessage,
    ChatResult,
    TextContent,
    ToolCallContent,
)
from ..core.event_stream import EventStream
from .base import Provider, ProviderConfig

if TYPE_CHECKING:
    import httpx


class OllamaProviderConfig(ProviderConfig):
    """Ollama Provider 配置."""

    model_config = ConfigDict(frozen=True)

    model: str = Field(default="llama3.2")
    base_url: str = Field(default="http://localhost:11434")
    temperature: Optional[float] = Field(default=None)
    max_tokens: Optional[int] = Field(default=None)
    top_p: Optional[float] = Field(default=None)


class OllamaProvider(Provider):
    """Ollama 本地模型提供商实现.

    支持本地部署的 LLM 模型，通过 Ollama HTTP API 访问。

    Usage:
        from tui_chatbot.provider import OllamaProvider, OllamaProviderConfig

        config = OllamaProviderConfig(model="llama3.2", base_url="http://localhost:11434")
        provider = OllamaProvider(config)

        stream = await provider.stream_chat(
            model="llama3.2",
            messages=[{"role": "user", "content": "Hello"}]
        )

        async for event in stream:
            print(event)
    """

    def __init__(
        self,
        config: Optional[OllamaProviderConfig] = None,
        client: Optional["httpx.AsyncClient"] = None,
    ):
        self._config = config or OllamaProviderConfig()
        self._client = client

    @property
    def name(self) -> str:
        return "ollama"

    @property
    def api_type(self) -> str:
        return "ollama"

    async def _get_client(self) -> "httpx.AsyncClient":
        if self._client is None:
            import httpx
            self._client = httpx.AsyncClient(base_url=self._config.base_url, timeout=300.0)
        return self._client

    async def stream_chat(
        self,
        model: str,
        messages: List[dict],
        tools: Optional[List[dict]] = None,
        signal: Optional[Any] = None,
        temperature: Optional[float] = None,
        max_tokens: Optional[int] = None,
    ) -> EventStream[AgentEvent, ChatResult]:
        """流式对话实现."""
        stream = EventStream[AgentEvent, ChatResult]()

        async def _stream():
            try:
                if signal and signal.aborted:
                    stream.end(ChatResult(messages=[]))
                    return

                client = await self._get_client()

                # 构建 Ollama 请求体
                payload = {
                    "model": model or self._config.model,
                    "messages": messages,
                    "stream": True,
                }

                # 添加可选参数
                options = {}
                temp = temperature if temperature is not None else self._config.temperature
                if temp is not None:
                    options["temperature"] = temp

                max_tok = max_tokens if max_tokens is not None else self._config.max_tokens
                if max_tok is not None:
                    options["num_predict"] = max_tok

                if self._config.top_p is not None:
                    options["top_p"] = self._config.top_p

                if options:
                    payload["options"] = options

                # Ollama 的工具支持
                if tools:
                    payload["tools"] = tools

                # 发送流式请求
                async with client.stream(
                    "POST",
                    "/api/chat",
                    json=payload,
                ) as response:
                    stream.push(AgentEvent(type=AgentEventType.MESSAGE_START))

                    content_parts: List[str] = []
                    message_started = True

                    async for line in response.aiter_lines():
                        if signal and signal.aborted:
                            raise asyncio.CancelledError("Aborted")

                        if not line:
                            continue

                        try:
                            data = json.loads(line)
                        except json.JSONDecodeError:
                            continue

                        # 处理消息增量
                        message = data.get("message", {})
                        content = message.get("content", "")

                        if content:
                            content_parts.append(content)
                            stream.push(
                                AgentEvent(
                                    type=AgentEventType.MESSAGE_UPDATE,
                                    partial_result={
                                        "type": "content",
                                        "content": content,
                                    },
                                )
                            )

                        # 检查是否完成
                        if data.get("done", False):
                            break

                # 构建最终消息
                final_content = "".join(content_parts)
                assistant_message = AssistantMessage(
                    content=[TextContent(text=final_content)] if final_content else [],
                    stop_reason="stop",
                )

                stream.push(
                    AgentEvent(
                        type=AgentEventType.MESSAGE_END,
                        message=assistant_message,
                    )
                )

                stream.push(
                    AgentEvent(
                        type=AgentEventType.TURN_END,
                        message=assistant_message,
                        tool_results=[],
                    )
                )

                result = ChatResult(
                    messages=[assistant_message],
                    finish_reason="stop",
                )
                stream.end(result)

            except asyncio.CancelledError:
                final_content = "".join(content_parts) if "content_parts" in dir() else ""
                if final_content:
                    assistant_message = AssistantMessage(
                        content=[TextContent(text=final_content)],
                        stop_reason="cancelled",
                    )
                    stream.push(
                        AgentEvent(
                            type=AgentEventType.MESSAGE_END,
                            message=assistant_message,
                        )
                    )
                    stream.end(
                        ChatResult(
                            messages=[assistant_message], finish_reason="cancelled"
                        )
                    )
                else:
                    stream.end(ChatResult(messages=[], finish_reason="cancelled"))
                raise

            except Exception as e:
                stream.push(
                    AgentEvent(
                        type=AgentEventType.ERROR,
                        error=str(e),
                    )
                )
                stream.error(e)

        asyncio.create_task(_stream())
        return stream

    async def list_models(self) -> List[str]:
        """列出本地可用的 Ollama 模型."""
        try:
            client = await self._get_client()
            response = await client.get("/api/tags")
            data = response.json()
            return [m["name"] for m in data.get("models", [])]
        except Exception:
            return []
```

**Provider 注册**:
在 `src/tui_chatbot/provider/registry.py` 的 `register_default_providers()` 中添加:

```python
def register_default_providers() -> None:
    # ... 现有代码 ...
    
    # 尝试注册 Ollama (本地模型)
    ollama_base_url = os.getenv("OLLAMA_BASE_URL", "http://localhost:11434")
    try:
        from .ollama_provider import OllamaProvider, OllamaProviderConfig
        
        config = OllamaProviderConfig(base_url=ollama_base_url)
        provider = OllamaProvider(config)
        ProviderRegistry.register("ollama", provider)
    except Exception:
        pass  # Ollama 未启动或不可用
```

---

### 2.4 新增内置工具设计

参考 `pi-mono` 的 `calculate` 和 `get-current-time` 工具实现。

#### 2.4.1 Calculator 工具

**文件**: `src/tui_chatbot/agent/tool.py` (已有，添加新类)

```python
class CalculatorParams(ToolParameters):
    """计算器参数."""
    
    expression: str = Field(
        description="要计算的数学表达式，例如 '2 + 2' 或 'sin(pi/2)'"
    )


class CalculatorTool(Tool):
    """数学表达式计算工具.
    
    支持基本数学运算、科学计算函数。
    """
    
    @property
    def name(self) -> str:
        return "calculate"
    
    @property
    def description(self) -> str:
        return "计算数学表达式的结果，支持 +, -, *, /, **, sin, cos, log, sqrt 等运算"
    
    @property
    def parameters(self):
        return CalculatorParams
    
    async def execute(self, params: CalculatorParams, signal=None) -> ToolResult:
        try:
            # 安全计算：限制可用函数
            allowed_names = {
                "abs": abs, "max": max, "min": min,
                "sum": sum, "pow": pow, "round": round,
                "sin": lambda x: __import__("math").sin(x),
                "cos": lambda x: __import__("math").cos(x),
                "tan": lambda x: __import__("math").tan(x),
                "sqrt": lambda x: __import__("math").sqrt(x),
                "log": lambda x: __import__("math").log(x),
                "log10": lambda x: __import__("math").log10(x),
                "exp": lambda x: __import__("math").exp(x),
                "pi": __import__("math").pi,
                "e": __import__("math").e,
            }
            
            # 计算表达式
            result = eval(params.expression, {"__builtins__": {}}, allowed_names)
            
            return ToolResult(
                content=f"{params.expression} = {result}",
                details={"expression": params.expression, "result": result}
            )
        except Exception as e:
            return ToolResult(
                content=f"计算错误: {e}",
                is_error=True,
                details={"error": str(e)}
            )
```

#### 2.4.2 ReadFile 工具

```python
class ReadFileParams(ToolParameters):
    """读取文件参数."""
    
    file_path: str = Field(description="要读取的文件路径")
    limit: Optional[int] = Field(default=1000, description="最大读取行数，默认 1000")


class ReadFileTool(Tool):
    """文件读取工具."""
    
    @property
    def name(self) -> str:
        return "read_file"
    
    @property
    def description(self) -> str:
        return "读取指定文件的内容，支持文本文件"
    
    @property
    def parameters(self):
        return ReadFileParams
    
    async def execute(self, params: ReadFileParams, signal=None) -> ToolResult:
        try:
            from pathlib import Path
            
            path = Path(params.file_path).expanduser()
            
            if not path.exists():
                return ToolResult(
                    content=f"文件不存在: {params.file_path}",
                    is_error=True
                )
            
            if not path.is_file():
                return ToolResult(
                    content=f"路径不是文件: {params.file_path}",
                    is_error=True
                )
            
            # 读取文件
            content = path.read_text(encoding="utf-8", errors="replace")
            
            # 限制行数
            lines = content.split("\n")
            if len(lines) > params.limit:
                lines = lines[:params.limit]
                lines.append(f"\n... (已截断，仅显示前 {params.limit} 行)")
                content = "\n".join(lines)
            
            return ToolResult(
                content=content,
                details={
                    "file_path": str(path),
                    "size": path.stat().st_size,
                    "lines": len(lines)
                }
            )
        except Exception as e:
            return ToolResult(
                content=f"读取文件失败: {e}",
                is_error=True,
                details={"error": str(e)}
            )
```

#### 2.4.3 WriteFile 工具

```python
class WriteFileParams(ToolParameters):
    """写入文件参数."""
    
    file_path: str = Field(description="要写入的文件路径")
    content: str = Field(description="文件内容")
    append: bool = Field(default=False, description="是否追加模式，默认覆盖")


class WriteFileTool(Tool):
    """文件写入工具."""
    
    @property
    def name(self) -> str:
        return "write_file"
    
    @property
    def description(self) -> str:
        return "将内容写入指定文件，支持创建新文件或覆盖/追加现有文件"
    
    @property
    def parameters(self):
        return WriteFileParams
    
    async def execute(self, params: WriteFileParams, signal=None) -> ToolResult:
        try:
            from pathlib import Path
            
            path = Path(params.file_path).expanduser()
            
            # 确保父目录存在
            path.parent.mkdir(parents=True, exist_ok=True)
            
            # 写入文件
            mode = "a" if params.append else "w"
            with open(path, mode, encoding="utf-8") as f:
                f.write(params.content)
            
            return ToolResult(
                content=f"文件已{'追加到' if params.append else '写入'}: {path}",
                details={
                    "file_path": str(path),
                    "size": path.stat().st_size,
                    "mode": "append" if params.append else "write"
                }
            )
        except Exception as e:
            return ToolResult(
                content=f"写入文件失败: {e}",
                is_error=True,
                details={"error": str(e)}
            )
```

#### 2.4.4 更新工具注册

修改 `src/tui_chatbot/agent/tool.py` 中的 `create_default_tool_registry`:

```python
def create_default_tool_registry() -> ToolRegistry:
    """创建包含默认工具的注册表."""
    registry = ToolRegistry()
    registry.register(GetCurrentTimeTool())
    registry.register(CalculatorTool())
    registry.register(ReadFileTool())
    registry.register(WriteFileTool())
    return registry
```

---

## 3. 文件创建/修改清单

### 3.1 修改文件

| 文件路径 | 修改内容 | 行数估算 |
|----------|----------|----------|
| `pyproject.toml` | 添加 pytest-asyncio 配置 | +3 |
| `src/tui_chatbot/provider/base.py` | stream_chat 添加 temperature/max_tokens 参数 | +2 |
| `src/tui_chatbot/provider/openai_provider.py` | 实现参数传递逻辑 | +10 |
| `src/tui_chatbot/provider/anthropic_provider.py` | 实现参数传递逻辑 | +10 |
| `src/tui_chatbot/agent/loop.py` | _stream_assistant_response 传递参数 | +2 |
| `src/tui_chatbot/agent/tool.py` | 添加 CalculatorTool, ReadFileTool, WriteFileTool | +120 |
| `src/tui_chatbot/provider/registry.py` | 添加 OllamaProvider 注册 | +10 |

### 3.2 新建文件

| 文件路径 | 内容说明 | 行数估算 |
|----------|----------|----------|
| `src/tui_chatbot/provider/ollama_provider.py` | OllamaProvider 完整实现 | ~250 |
| `tests/test_ollama_provider.py` | OllamaProvider 单元测试 | ~80 |
| `tests/test_new_tools.py` | 新工具单元测试 | ~100 |

---

## 4. 测试策略

### 4.1 pytest-asyncio 测试验证

```bash
# 运行之前失败的测试
PYTHONPATH=src python -m pytest tests/test_improvements.py tests/test_integration.py -v
```

**预期结果**: 15 个测试全部通过

### 4.2 temperature/max_tokens 测试

**测试文件**: `tests/test_agent_loop.py` (已有)

现有测试 `test_temperature_and_max_tokens` 应该通过，验证参数正确传递。

### 4.3 OllamaProvider 测试

**测试文件**: `tests/test_ollama_provider.py`

测试内容:
1. 基本初始化测试
2. list_models() 测试 (mock)
3. stream_chat() 基本流程测试 (mock)
4. 参数传递测试 (temperature, max_tokens)

### 4.4 新工具测试

**测试文件**: `tests/test_new_tools.py`

测试内容:
1. CalculatorTool - 基本计算、科学函数、错误处理
2. ReadFileTool - 文件读取、行数限制、错误处理
3. WriteFileTool - 文件写入、追加模式、目录创建

---

## 5. 验收标准

### 5.1 必达标准 (MVP)

| 验收项 | 标准 | 验证方式 |
|--------|------|----------|
| pytest-asyncio 配置 | 15 个失败测试修复 | `pytest tests/test_improvements.py tests/test_integration.py` |
| temperature/max_tokens 传递 | AgentLoopConfig 参数传递到 Provider 调用 | 代码审查 + 单元测试 |
| OllamaProvider | 基本功能可用，可列出本地模型 | 单元测试 |
| 新工具 | Calculator, ReadFile, WriteFile 可用 | 单元测试 |

### 5.2 理想标准

| 验收项 | 标准 |
|--------|------|
| 整体测试通过率 | ≥ 95% (199+15=214 通过) |
| 代码覆盖率 | agent/loop.py 保持 ≥ 85% |
| 向后兼容 | 现有 API 无破坏性变更 |

### 5.3 已知问题处理

以下测试失败可接受在本轮不修复 (优先级低):

| 问题 | 影响 | 处理方案 |
|------|------|----------|
| AgentEvent 允许额外字段 | 数据验证宽松 | N=4 或后续修复 |
| ChatResult 非 frozen | 数据不变性 | N=4 或后续修复 |
| TokenStats.elapsed 计算 | 边界情况 | N=4 或后续修复 |

---

## 6. 架构一致性检查

### 6.1 与 pi-mono 参考对比

| 本项目实现 | pi-mono 参考 | 一致性 |
|------------|--------------|--------|
| Agent Loop 双层循环 | agent-loop.ts | ✅ 一致 |
| EventStream 事件流 | EventStream | ✅ 一致 |
| Tool 抽象 | AgentTool | ✅ 一致 |
| Provider 基类 | Provider | ✅ 一致 |
| Calculator 工具 | calculate.ts | ✅ 一致 |

### 6.2 设计决策

1. **Ollama 使用 httpx**: 选择 `httpx` 而非 `requests` 因为它原生支持 async
2. **参数传递优先级**: 调用时参数 > config 参数 > provider 默认参数
3. **工具安全性**: Calculator 使用受限的 eval，只允许数学函数
4. **文件操作限制**: 工具本身不做路径限制，由调用者控制权限

---

## 7. 实施建议

### 7.1 建议执行顺序

1. **Engineer 1**: pytest-asyncio 配置修复 (pyproject.toml)
2. **Engineer 2**: temperature/max_tokens 传递 (base.py, openai_provider.py, anthropic_provider.py, loop.py)
3. **Engineer 3**: OllamaProvider (ollama_provider.py + 注册)
4. **Engineer 4**: 新工具 (tool.py 添加 CalculatorTool, ReadFileTool, WriteFileTool)

### 7.2 依赖关系

```
Engineer 1 (pytest-asyncio) ──┐
                                ├──► Reviewer 评审
Engineer 2 (参数传递) ──────────┤
                                │
Engineer 3 (Ollama) ────────────┤
                                │
Engineer 4 (新工具) ────────────┘
                                │
                                ▼
                        IntegrationTester 集成测试
                        Tester 单元测试
                                │
                                ▼
                        Designer 审批
```

---

## 8. 总结

本轮迭代 (N=3) 聚焦:
1. **修复测试配置** - 解决 15 个测试失败，提升测试稳定性
2. **完成功能集成** - temperature/max_tokens 参数传递，提升配置灵活性
3. **扩展 Provider 支持** - OllamaProvider 实现本地模型支持
4. **增强工具系统** - 添加计算器、文件读写等实用工具

预计新增代码: ~500 行
预计修改代码: ~30 行

**方案批准**: 待 Engineer 实现后由 Reviewer 和 Tester 验证

---

*报告路径*: `/docs/designer/3.md`  
*参考文档*:
- `/docs/approval/2.md` (上一轮审批)
- `/docs/tester/2.md` (上一轮测试报告)
- `refs/pi-mono/packages/agent/src/agent-loop.ts` (参考实现)
