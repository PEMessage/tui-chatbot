# 第 7 轮迭代技术方案 (N=7)

**迭代版本**: N=7  
**设计角色**: Designer Agent  
**设计日期**: 2026-04-09  
**状态**: 📝 设计方案  

---

## 1. 本轮迭代目标

### 1.1 问题背景

用户报告了运行时错误：

```
File "main.py", line 224, in __init__
    storage = SessionStorage()
              ^^^^^^^^^^^^^^^^
TypeError: SessionStorage.__init__() missing 1 required positional argument: 'storage_dir'
```

**根本原因**: `SessionStorage` 类在实例化时缺少必需的 `storage_dir` 参数，导致 CLI 无法启动。

### 1.2 总体目标

本轮迭代聚焦于 **Bug 修复** 和 **代码健壮性增强**，确保 CLI 能正常启动并运行。

| 维度 | 目标 | 预期成果 |
|------|------|----------|
| Bug 修复 | 修复 SessionStorage 初始化错误 | CLI 正常启动 |
| 代码健壮性 | 为所有管理类添加默认值 | 避免类似初始化错误 |
| 回归防护 | 添加启动测试 | 防止启动失败问题复发 |

### 1.3 具体目标清单

#### 目标 1: 修复 SessionStorage 初始化 (高优先级)

| ID | 任务 | 说明 | 状态 |
|----|------|------|------|
| 1.1 | 添加默认路径支持 | `storage_dir` 参数设为可选 | 待实现 |
| 1.2 | 自动创建存储目录 | 默认路径 `~/.config/tui-chatbot/sessions` | 待实现 |
| 1.3 | 验证修复 | 确保 `SessionStorage()` 可正常实例化 | 待实现 |

#### 目标 2: 修复其他潜在初始化问题 (高优先级)

| ID | 模块 | 检查项 | 状态 |
|----|------|--------|------|
| 2.1 | SessionManager | 检查是否需要默认存储路径 | 待检查 |
| 2.2 | 其他管理类 | 检查是否有类似问题 | 待检查 |
| 2.3 | main.py | 确保所有实例化调用正确 | 待验证 |

#### 目标 3: 代码重构 - 统一初始化模式 (中优先级)

| ID | 任务 | 说明 | 参考 |
|----|------|------|------|
| 3.1 | 统一构造函数风格 | 所有管理类使用 Optional[Path] = None | ConfigManager |
| 3.2 | 提取默认路径常量 | 避免硬编码路径 | pi-mono 风格 |
| 3.3 | 添加类型注解 | 确保 IDE 能正确提示 | Python 最佳实践 |

#### 目标 4: 启动测试 (中优先级)

| ID | 测试类型 | 说明 | 验证内容 |
|----|----------|------|----------|
| 4.1 | 单元测试 | 测试各模块初始化 | 所有管理类可实例化 |
| 4.2 | 集成测试 | 测试 CLI 启动 | App 类可正常创建 |
| 4.3 | 冒烟测试 | 验证基本功能可用 | 命令解析正常 |

---

## 2. 详细设计规范

### 2.1 SessionStorage 修复设计

#### 2.1.1 当前问题代码

```python
# src/tui_chatbot/session/storage.py
class SessionStorage:
    """会话存储管理."""

    def __init__(self, storage_dir: Path):  # ❌ 缺少默认值
        self._storage_dir = storage_dir
        self._storage_dir.mkdir(parents=True, exist_ok=True)
```

#### 2.1.2 修复方案

参考 `ConfigManager` 的实现模式：

```python
# src/tui_chatbot/session/storage.py
class SessionStorage:
    """会话存储管理."""

    def __init__(self, storage_dir: Optional[Path] = None):  # ✅ 添加默认值
        if storage_dir is None:
            storage_dir = Path.home() / ".config" / "tui-chatbot" / "sessions"
        self._storage_dir = storage_dir
        self._storage_dir.mkdir(parents=True, exist_ok=True)
```

#### 2.1.3 完整修复代码

```python
"""会话存储管理."""

from __future__ import annotations

import json
from pathlib import Path
from typing import List, Optional

from .models import ChatSession


class SessionStorage:
    """会话存储管理."""

    def __init__(self, storage_dir: Optional[Path] = None):
        """初始化会话存储.
        
        Args:
            storage_dir: 存储目录路径，默认为 ~/.config/tui-chatbot/sessions
        """
        if storage_dir is None:
            storage_dir = Path.home() / ".config" / "tui-chatbot" / "sessions"
        self._storage_dir = storage_dir
        self._storage_dir.mkdir(parents=True, exist_ok=True)

    def save(self, session: ChatSession) -> None:
        """保存会话到文件."""
        file_path = self._storage_dir / f"{session.metadata.id}.json"
        with open(file_path, "w", encoding="utf-8") as f:
            json.dump(session.model_dump(), f, indent=2, default=str)

    def load(self, session_id: str) -> Optional[ChatSession]:
        """加载会话."""
        file_path = self._storage_dir / f"{session_id}.json"
        if not file_path.exists():
            return None
        with open(file_path, encoding="utf-8") as f:
            data = json.load(f)
        return ChatSession.model_validate(data)

    def list_all(self) -> List[ChatSession]:
        """列出所有会话."""
        sessions = []
        for file_path in self._storage_dir.glob("*.json"):
            with open(file_path, encoding="utf-8") as f:
                data = json.load(f)
            sessions.append(ChatSession.model_validate(data))
        return sorted(sessions, key=lambda s: s.metadata.updated_at, reverse=True)
```

### 2.2 统一初始化模式设计

#### 2.2.1 模式对比

| 模块 | 当前实现 | 建议修改 |
|------|----------|----------|
| ConfigManager | ✅ `config_dir: Optional[Path] = None` | 无需修改 |
| SessionStorage | ❌ `storage_dir: Path` | 添加默认值 |
| SessionManager | 待检查 | 检查是否需要修改 |

#### 2.2.2 默认路径常量设计

```python
# src/tui_chatbot/constants.py
"""全局常量定义."""

from pathlib import Path

# 基础配置目录
CONFIG_DIR = Path.home() / ".config" / "tui-chatbot"

# 子目录
SESSIONS_DIR = CONFIG_DIR / "sessions"
EXPORTS_DIR = CONFIG_DIR / "exports"
CACHE_DIR = CONFIG_DIR / "cache"

# 配置文件
CONFIG_FILE = CONFIG_DIR / "config.json"
```

#### 2.2.3 ConfigManager 更新

```python
# src/tui_chatbot/config/manager.py
from ..constants import CONFIG_DIR, CONFIG_FILE

class ConfigManager:
    """配置管理器"""

    def __init__(self, config_dir: Optional[Path] = None):
        if config_dir is None:
            config_dir = CONFIG_DIR
        self._config_dir = config_dir
        self._config_dir.mkdir(parents=True, exist_ok=True)
        self._config_file = self._config_dir / "config.json"
        self._config = self._load()
```

#### 2.2.4 SessionStorage 更新（使用常量）

```python
# src/tui_chatbot/session/storage.py
from ..constants import SESSIONS_DIR

class SessionStorage:
    """会话存储管理."""

    def __init__(self, storage_dir: Optional[Path] = None):
        if storage_dir is None:
            storage_dir = SESSIONS_DIR
        self._storage_dir = storage_dir
        self._storage_dir.mkdir(parents=True, exist_ok=True)
```

### 2.3 启动测试设计

#### 2.3.1 模块初始化测试

```python
# tests/test_startup.py
"""启动测试 - 验证 CLI 能正常初始化."""

import pytest
from pathlib import Path


class TestModuleInitialization:
    """测试各模块初始化."""

    def test_session_storage_default_init(self):
        """SessionStorage 默认初始化."""
        from tui_chatbot.session.storage import SessionStorage
        
        storage = SessionStorage()  # 不应抛出异常
        assert storage._storage_dir is not None
        assert storage._storage_dir.exists()

    def test_session_storage_custom_path(self):
        """SessionStorage 自定义路径."""
        from tui_chatbot.session.storage import SessionStorage
        import tempfile
        
        with tempfile.TemporaryDirectory() as tmpdir:
            custom_path = Path(tmpdir) / "sessions"
            storage = SessionStorage(storage_dir=custom_path)
            assert storage._storage_dir == custom_path
            assert custom_path.exists()

    def test_config_manager_default_init(self):
        """ConfigManager 默认初始化."""
        from tui_chatbot.config.manager import ConfigManager
        
        manager = ConfigManager()
        assert manager._config_dir is not None
        assert manager._config_file is not None

    def test_session_manager_init(self):
        """SessionManager 初始化."""
        from tui_chatbot.session.manager import SessionManager
        from tui_chatbot.session.storage import SessionStorage
        
        storage = SessionStorage()
        manager = SessionManager(storage)
        assert manager is not None
```

#### 2.3.2 CLI 启动测试

```python
# tests/test_startup.py

class TestCLIStartup:
    """测试 CLI 启动."""

    def test_app_import(self):
        """App 类可导入."""
        from tui_chatbot.main import App
        assert App is not None

    def test_app_basic_creation(self, monkeypatch):
        """App 类可实例化（模拟环境）."""
        from tui_chatbot.main import App
        
        # 模拟环境变量，避免需要真实 API key
        monkeypatch.setenv("OPENAI_API_KEY", "test-key")
        
        # 由于 App.__init__ 依赖较多，这里仅测试不崩溃
        # 实际测试需要更多 mocking
        try:
            # 这个测试可能会失败，但至少能暴露问题
            app = App()
            assert app is not None
        except Exception as e:
            # 记录错误，但不一定是测试失败
            # 因为我们可能没有完整的运行环境
            pytest.skip(f"App 实例化需要完整环境: {e}")
```

#### 2.3.3 冒烟测试

```python
# tests/test_startup.py

class TestSmoke:
    """冒烟测试 - 验证基本功能."""

    def test_import_all_modules(self):
        """所有模块可导入."""
        import tui_chatbot
        import tui_chatbot.session
        import tui_chatbot.session.storage
        import tui_chatbot.session.manager
        import tui_chatbot.session.models
        import tui_chatbot.config
        import tui_chatbot.config.manager
        import tui_chatbot.agent
        import tui_chatbot.provider
        # 如果导入成功，测试通过
        assert True

    def test_session_storage_operations(self):
        """SessionStorage 基本操作."""
        from tui_chatbot.session.storage import SessionStorage
        from tui_chatbot.session.models import ChatSession, SessionMetadata
        from datetime import datetime
        import tempfile
        
        with tempfile.TemporaryDirectory() as tmpdir:
            storage = SessionStorage(storage_dir=Path(tmpdir) / "sessions")
            
            # 创建测试会话
            session = ChatSession(
                metadata=SessionMetadata(
                    id="test-session",
                    title="Test Session",
                    model="gpt-4",
                    created_at=datetime.now(),
                    updated_at=datetime.now(),
                ),
                messages=[]
            )
            
            # 保存
            storage.save(session)
            
            # 加载
            loaded = storage.load("test-session")
            assert loaded is not None
            assert loaded.metadata.id == "test-session"
            
            # 列出
            sessions = storage.list_all()
            assert len(sessions) == 1
```

---

## 3. 文件变更清单

### 3.1 新增文件

| 文件路径 | 类型 | 说明 | 预估行数 |
|----------|------|------|----------|
| `src/tui_chatbot/constants.py` | 新增 | 全局常量定义（默认路径） | 15 |
| `tests/test_startup.py` | 新增 | 启动和冒烟测试 | 100 |

### 3.2 修改文件

| 文件路径 | 变更类型 | 变更内容 | 预估行数 |
|----------|----------|----------|----------|
| `src/tui_chatbot/session/storage.py` | 修改 | 添加默认路径支持 | +8 |
| `src/tui_chatbot/config/manager.py` | 修改 | 使用 constants 中的路径 | +3, -2 |
| `src/tui_chatbot/__init__.py` | 修改 | 导出 constants | +5 |

---

## 4. 测试策略

### 4.1 单元测试

#### 4.1.1 SessionStorage 默认初始化测试

```python
def test_session_storage_default_path():
    """默认路径为 ~/.config/tui-chatbot/sessions."""
    from tui_chatbot.session.storage import SessionStorage
    from pathlib import Path
    
    storage = SessionStorage()
    expected = Path.home() / ".config" / "tui-chatbot" / "sessions"
    assert storage._storage_dir == expected
```

#### 4.1.2 目录自动创建测试

```python
def test_storage_dir_auto_created():
    """存储目录自动创建."""
    from tui_chatbot.session.storage import SessionStorage
    import tempfile
    import shutil
    
    with tempfile.TemporaryDirectory() as tmpdir:
        storage_dir = Path(tmpdir) / "new" / "nested" / "sessions"
        assert not storage_dir.exists()
        
        storage = SessionStorage(storage_dir=storage_dir)
        assert storage_dir.exists()
```

### 4.2 集成测试

#### 4.2.1 main.py 实例化测试

```python
def test_main_session_storage_init():
    """main.py 中的 SessionStorage 实例化."""
    # 验证 main.py 第 224 行不会报错
    from tui_chatbot.session.storage import SessionStorage
    
    # 这是 main.py 中使用的调用方式
    storage = SessionStorage()  # 不应抛出 TypeError
    assert storage is not None
```

### 4.3 冒烟测试

#### 4.3.1 快速启动验证

```python
def test_quick_startup():
    """快速验证能启动到命令解析阶段."""
    import subprocess
    import sys
    
    # 尝试导入主模块
    result = subprocess.run(
        [sys.executable, "-c", "from tui_chatbot.main import App; print('OK')"],
        capture_output=True,
        text=True
    )
    
    assert result.returncode == 0
    assert "OK" in result.stdout
```

---

## 5. 验收标准

### 5.1 必达标准

| 验收项 | 验收标准 | 验证方法 |
|--------|----------|----------|
| Bug 修复 | `SessionStorage()` 可正常实例化 | pytest test_startup.py |
| 默认路径 | 未提供参数时使用 `~/.config/tui-chatbot/sessions` | 单元测试 |
| 目录创建 | 自动创建不存在的目录 | 单元测试 |
| CLI 启动 | main.py 不再报 TypeError | 手动测试 |
| 回归测试 | 所有现有测试仍通过 | pytest |

### 5.2 理想标准

| 验收项 | 验收标准 | 验证方法 |
|--------|----------|----------|
| 代码覆盖率 | 新增代码覆盖 ≥ 90% | coverage 报告 |
| 测试速度 | 启动测试 < 5s | pytest |
| 常量导出 | constants 模块正确导出 | import 测试 |
| 文档完整 | 代码注释完整 | 静态检查 |

---

## 6. 模块导出设计

### 6.1 constants.py 创建

```python
# src/tui_chatbot/constants.py
"""全局常量定义."""

from pathlib import Path

# 基础配置目录
CONFIG_DIR = Path.home() / ".config" / "tui-chatbot"

# 子目录
SESSIONS_DIR = CONFIG_DIR / "sessions"
EXPORTS_DIR = CONFIG_DIR / "exports"
CACHE_DIR = CONFIG_DIR / "cache"

# 配置文件
CONFIG_FILE = CONFIG_DIR / "config.json"
```

### 6.2 __init__.py 更新

```python
# src/tui_chatbot/__init__.py

# 新增常量导出
from .constants import (
    CONFIG_DIR,
    SESSIONS_DIR,
    EXPORTS_DIR,
    CACHE_DIR,
    CONFIG_FILE,
)

__all__ = [
    # ... 现有导出 ...
    # 常量
    "CONFIG_DIR",
    "SESSIONS_DIR",
    "EXPORTS_DIR",
    "CACHE_DIR",
    "CONFIG_FILE",
]
```

---

## 7. 实现顺序建议

```
阶段 1: Bug 修复（高优先级）
  1. 修改 SessionStorage 添加默认参数
  2. 运行手动测试验证修复
  3. 确保 main.py 能正常启动

阶段 2: 代码重构（中优先级）
  4. 创建 constants.py 模块
  5. 更新 ConfigManager 使用常量
  6. 更新 SessionStorage 使用常量
  7. 更新 __init__.py 导出常量

阶段 3: 测试（中优先级）
  8. 创建 tests/test_startup.py
  9. 添加 SessionStorage 测试
  10. 添加启动测试
  11. 运行全部测试验证

阶段 4: 验收
  12. 运行回归测试
  13. 手动验证 CLI 启动
  14. 提交代码
```

---

## 8. 参考文档

| 文档 | 路径 | 说明 |
|------|------|------|
| N=6 设计方案 | `/docs/designer/6.md` | 上一轮的详细设计 |
| N=6 审批报告 | `/docs/approval/6.md` | 当前架构状态 |
| ConfigManager | `/src/tui_chatbot/config/manager.py` | 参考实现模式 |
| SessionStorage | `/src/tui_chatbot/session/storage.py` | 待修复的模块 |
| main.py | `/src/tui_chatbot/main.py` | 错误发生位置 |
| pi-mono agent | `/refs/pi-mono/packages/agent/src/agent.ts` | 模块化架构参考 |
| pi-mono ai | `/refs/pi-mono/packages/ai/src/index.ts` | 导出模式参考 |

---

## 9. 风险与缓解

| 风险 | 可能性 | 影响 | 缓解措施 |
|------|--------|------|----------|
| 修复不完整 | 低 | 高 | 全面测试所有初始化路径 |
| 路径权限问题 | 低 | 中 | 使用 exist_ok=True，处理 PermissionError |
| 与其他模块冲突 | 低 | 中 | 保持向后兼容，Optional 参数 |
| 测试环境差异 | 中 | 低 | 使用 tempfile，不依赖真实 home 目录 |

---

## 10. 快速修复验证

修复后，以下代码应能正常执行：

```python
# 验证脚本
from tui_chatbot.session.storage import SessionStorage
from tui_chatbot.session.manager import SessionManager

# 1. SessionStorage 默认初始化
storage = SessionStorage()
print(f"✓ SessionStorage 默认初始化成功: {storage._storage_dir}")

# 2. SessionManager 使用默认 storage
manager = SessionManager(storage)
print(f"✓ SessionManager 初始化成功")

# 3. 模拟 main.py 的调用
from pathlib import Path
import tempfile

with tempfile.TemporaryDirectory() as tmpdir:
    # 自定义路径也应工作
    custom_storage = SessionStorage(storage_dir=Path(tmpdir) / "sessions")
    print(f"✓ SessionStorage 自定义路径成功: {custom_storage._storage_dir}")

print("\n所有测试通过！Bug 已修复。")
```

---

**设计方案版本**: 1.0  
**最后更新**: 2026-04-09  
**Designer**: Agent Designer
